export type Movie = {
    title: string;
    opening_crawl:string,
    release_date:string,
    characters:[string]
}

export type CharacterType = {
    name:string;
    gender:string;
    height:string
}